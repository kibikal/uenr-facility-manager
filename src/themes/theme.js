import { createTheme } from "@mui/material";

const globalTheme = createTheme({
  palette: {
    primary: {
      main: "#38E54D",
    },
    secondary: {
      main: "#632626",
      light: "#d7ccc8",
    },
    grayText: "#dd2c00",
  },
});

export default globalTheme;
